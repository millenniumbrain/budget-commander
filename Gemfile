source 'https://rubygems.org'

# => server
gem 'puma'

# => core
gem 'roda'

# => database
gem 'sequel'
gem 'pg'

# => security
gem 'bcrypt'

# => templates
gem 'builder'
gem 'tilt'
gem 'erubis'

# => other
gem 'oj'
gem 'rake'
gem 'chronic'
gem 'mini_magick'
gem "rack_csrf"
gem 'mail'
gem 'json'

group :development do
  gem 'sqlite3'
  gem 'sass'
  gem 'yard'
  gem 'shotgun'
end

group :testing do
  gem 'rack-test', require: 'rack/test'
  gem 'better_errors'
  gem 'minitest'
  gem 'capybara'
  gem "minitest-hooks"
end
